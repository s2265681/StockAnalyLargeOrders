---
description: 本地开发最快启动前后端（勿用 start.sh）
alwaysApply: true
---

# 本地开发最快启动

用户要求「启动前后端 / 启动服务 / 本地开发」时，**不要用 `./start.sh`**（会跑 pip/pnpm install，慢且前端易失败）。按下列最短路径：

## 0. 先检查是否已在跑

```bash
nc -z 127.0.0.1 3307          # 隧道
lsof -iTCP:9001 -sTCP:LISTEN  # 后端
lsof -iTCP:9000 -sTCP:LISTEN  # 前端
```

已在监听则跳过对应步骤；有代码改动才重启后端。

## 1. 数据库 SSH 隧道（必须先做）

```bash
./scripts/db-tunnel.sh status || ./scripts/db-tunnel.sh start
```

- `127.0.0.1:3307` → 服务器 MySQL；凭据 `scripts/local.deploy.env`
- `/health` 不查库；隧道断开时登录等接口报「请确认数据库连接」

## 2. 后端（9001）— 后台启动

```bash
cd backend && source venv/bin/activate
nohup python app.py >> ../backend.log 2>&1 &
```

不跑 `pip install`（依赖已装时）。

## 3. 前端（9000）— 勿用 pnpm start

`pnpm start` 会因 ESLint 插件冲突失败。用 `react-scripts` 直接启：

```bash
cd frontend
nohup ./node_modules/.bin/react-scripts start >> ../frontend.log 2>&1 &
```

需有 `frontend/.env.development.local`：

```
DISABLE_ESLINT_PLUGIN=true
PORT=9000
```

## 4. 验证

```bash
curl -s http://localhost:9001/health
curl -s -o /dev/null -w "%{http_code}" http://localhost:9000
```

## 5. 停止

`./stop.sh` 或 `pkill -f "python app.py"` / `pkill -f "react-scripts start"`
