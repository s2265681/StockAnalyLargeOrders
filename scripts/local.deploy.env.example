# 本地 SSH 隧道配置（复制为 local.deploy.env）
# cp scripts/local.deploy.env.example scripts/local.deploy.env

SERVER_HOST=47.99.130.21
SERVER_USER=root
SERVER_PORT=22
# 私钥路径，留空则用 ssh 默认密钥；若仅密码登录，先在终端执行一次 ssh root@47.99.130.21 保存凭据
# SSH_KEY=~/.ssh/id_ed25519_work

# 与 backend/.env 中 MYSQL_PORT 保持一致
LOCAL_DB_PORT=3307
REMOTE_DB_HOST=127.0.0.1
REMOTE_DB_PORT=3306
